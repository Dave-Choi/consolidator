<%= simple_form_for(@borrow_request) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :thing %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
