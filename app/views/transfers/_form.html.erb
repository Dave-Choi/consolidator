<%= simple_form_for(@transfer) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :datetime %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
